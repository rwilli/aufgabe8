/**
 * Class StonePit with type variable T extends Storage<T>.
 * 
 * @author Gruppe222
 * 
 * @param <T>
 *            type variable.
 */
public class StonePit<T> extends Storage<T> {

	/**
	 * Constructor calls the super constructor.
	 * 
	 * @param size
	 *            the size of the stonepit as Integer.
	 */
	public StonePit(int size) {
		super(size);
	}

}
